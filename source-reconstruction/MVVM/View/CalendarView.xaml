<!--
  HYPOTHETICAL RECONSTRUCTION — not the original CalendarView.xaml.
  CalendarView.g.cs reveals:
    line 13: <vm:CalendarViewModel x:Name="CVM" />            (id 1 — DataContext)
    line 17: <syncfusion:SfScheduler x:Name="Schedule" ... /> (id 2)
  So the View instantiates its own VM via XAML and hosts an SfScheduler bound
  to the VM's Appointments collection.
-->
<UserControl x:Class="Review_Reminder.MVVM.View.CalendarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             xmlns:vm="clr-namespace:Review_Reminder.MVVM.ViewModel"
             Background="#272537">
    <UserControl.DataContext>
        <vm:CalendarViewModel x:Name="CVM" />
    </UserControl.DataContext>

    <Grid Margin="16">
        <syncfusion:SfScheduler x:Name="Schedule"
                                ViewType="Week"
                                ItemsSource="{Binding Appointments}">
            <syncfusion:SfScheduler.AppointmentMapping>
                <syncfusion:AppointmentMapping
                    Subject="Subject"
                    StartTime="StartTime"
                    EndTime="EndTime"
                    Notes="Notes" />
            </syncfusion:SfScheduler.AppointmentMapping>
        </syncfusion:SfScheduler>
    </Grid>
</UserControl>
